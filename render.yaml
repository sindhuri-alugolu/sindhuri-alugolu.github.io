services:
  - type: web
    name: sindhu-portfolio
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /sindhu.html
    envVars:
      - key: NODE_ENV
        value: production
